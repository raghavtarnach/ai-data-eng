# Pinned dependencies for the execution sandbox.
# These are the ONLY packages available inside the sandbox container.
# No ad-hoc pip install is allowed at runtime.

pandas==2.2.1
numpy==1.26.4
pyarrow==15.0.0
sqlalchemy==2.0.27
openpyxl==3.1.2
python-dateutil==2.9.0
